FROM ubuntu:22.04

ENV DEBIAN_FRONTEND=noninteractive

RUN apt-get update -qq && apt-get install -y -qq --no-install-recommends \
    openssh-server \
    openvpn \
    iproute2 \
    && apt-get clean \
    && rm -rf /var/lib/apt/lists/*

RUN mkdir -p /var/run/sshd /etc/openvpn

EXPOSE 22 1194/udp

CMD ["/usr/sbin/sshd", "-D", "-e"]
